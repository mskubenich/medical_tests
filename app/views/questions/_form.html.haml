= form_for [@category, @question], :html => { :class => 'form-horizontal' } do |f|
  .form-group
    = f.label :text, :class => 'control-label col-md-2'
    .col-md-10
      = f.text_area :text, :class => 'text_field form-control', rows: 10
  .form-group
    .col-md-offset-2.col-md-10
      = f.submit nil, :class => 'btn btn-primary'
      = link_to t('.cancel', :default => t("helpers.links.cancel")), category_questions_path, :class => 'btn btn-default'